// 1. Primitive Types (মৌলিক ধরন)

// 👉 এগুলো single/simple value ধরে রাখে

// String → "Durjoy"

// Number → 25

// Boolean → true / false

// Undefined → কোনো value assign করা হয়নি

// Null → ইচ্ছা করে খালি করে রাখা

// Symbol → unique value (ES6 থেকে)

// BigInt → অনেক বড় সংখ্যা রাখার জন্য

// 2. Non-Primitive (Reference Types)

// 👉 এগুলো অনেক value একসাথে ধরে রাখতে পারে

//1. Object

//2. Array

//3. Function

// 🔹 String কী?

// 👉 String হলো টেক্সট (অক্ষর, শব্দ, বাক্য) রাখার জন্য ব্যবহার করা data type।
// JavaScript এ String quote (" ", ' ', বা ` `) এর মধ্যে লেখা হয়।

var name = " Durjoy"
var address = " shimulguri"

// console.log(name);

// 🔹 Boolean কী?

// 👉 Boolean হলো এমন একটা data type যেটা শুধু দুইটা মান (value) রাখতে পারে:

// 1.true ✅  2.false ❌  এটা ব্যবহার হয় হ্যাঁ/না, চালু/বন্ধ, সঠিক/ভুল এর মতো অবস্থা বোঝাতে।
// example
var isCaptain = false ;
var ispassed = true;

// console.log(ispassed);
// typeof chek korte 
// console.log(typeof(isCaptain));
// console.log (typeof address);
